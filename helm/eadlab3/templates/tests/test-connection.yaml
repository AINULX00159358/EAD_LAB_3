apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "eadlab3.fullname" . }}-test-connection"
  labels:
    {{- include "eadlab3.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "eadlab3.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
